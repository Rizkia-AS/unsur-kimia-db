const BarHeader = ({title}) => {
    return (
        <header>
            <span className="material-icons-outlined science">science</span>
            <h2>{title}</h2>
        </header>
    )
}

export default BarHeader